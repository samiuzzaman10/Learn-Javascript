// For of loop
const point = [45, 27, 73, 59, 32];
for (let element of point) {
  //console.log(element);
}

const names = "Samiuzzaman";
for(let n of names){
  // console.log(n);
}


// For in loop
const programmingLanguage = {
  language: "Javascript",
  library: "React js",
  framework: "Next js",
  estd: 1995,
  founder: "Brendan Eich",
};
for (let value in programmingLanguage) {
  // console.log(`${value} = ${programmingLanguage[value]}`);
}
